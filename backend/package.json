{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "handler.js",
  "scripts": {
    "start": "sls offline --port 8001",
    "deploy": "sls deploy -s prod -v ",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "serverless-offline": "^12.0.4"
  },
  "dependencies": {
    "aws-sdk": "^2.613.0",
    "request": "^2.88.0",
    "winston": "^3.2.1"
  }
}
